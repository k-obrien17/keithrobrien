# Contributor Notes

This is a static-first Next.js personal site.

- Keep content in checked-in JSON and MDX files.
- Do not add a database, auth layer, CMS, or private vault sync.
- Do not commit secrets, local `.env` files, internal handoff notes, or private strategy docs.
- Run `npm run lint` and `npm run build` before opening a PR.
